package main

import "fmt"

const PRODUCT string = "india"
const PRICE = 500

func main() {
	fmt.Println(product)
	fmt.Println(price)
	mul()
}

const (
	product  = "mobile"
	price    = 200
	quantity = 50
	stock    = true
)

func mul() {
	fmt.Println(product)
	fmt.Println(price)
	fmt.Println(quantity)
	fmt.Println(stock)
}
